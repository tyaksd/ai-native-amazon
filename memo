printful　api(最悪手動)
printfulの発送されたかを、orderにtrue, falseで保存

各ストアを運用するAIエージェント

godshipのx instagram運用AIエージェント

世の中のブランドと商品を大量に学習するai
トレンド定量化ai
クリエイティブ定量化ai

shopoifyのメールキャンペーンautomationに載っているメール字自動化を基本的に全部搭載

stripe本番

/ufuhとかでオーダーを見れるようにして

AIようのUI

将来的にその人がフォローしているsnsなどのアカウントを分析して、それらの
ブランドに変わるオリジナル意義なるブランドを作成

new brandは0.08ドル
new productは0.045ドル, 45 びょう
highなら0.17

Dashboard的なやつで、売り上げ、wow/momグローすぐらふ
2.64
/oiu、本番環境ではパスワード、ローカルホストではなし

チェックアウトされたらprintfulにアップロードして、画像位置調整して発注するシステム

ログイン機能
でその中でそのユーザーの情報を入力、お気に入りブランドとかurl入力、ますとでらないが、より好みに合わせたブランドと商品をその人用に生成できるようになる

カスタマーサポート　エージェント

商品、基本追加すると公開だが、/oiuで非公開にできるチェックボタンを追加

inside labelにその賞遺品のブランドのロゴ。


<＜ルール＞＞
カラーは全部大文字で


<<<<<<githubの変更がvercelでディプロイしているコードに自動で反映されない時>>>>>>>>
1. Git に正しい author を設定
まず GitHub に登録しているメールを確認します（例：yourname@gmail.com や xxxx@users.noreply.github.com）。
git config --global user.name  "あなたの名前"
git config --global user.email "GitHubに登録しているメール"
2. 設定を確認
git config --global --list
で user.email が GitHub のメールになっているか確認。
3. コミットを修正
すでに push したコミットが「.local」になっているので、最新のコミットだけ直したいなら：
# 最新コミットの author を修正
git commit --amend --reset-author --no-edit
# 修正を force push
git push origin main -f
これで最新コミットの author が正しい GitHub メールに置き換わります。
<<<<<<<<<<>>>>>>>>>>

<<<<<教訓>>>>>>>.
データを新しく作るときはsupabase sqlコードを忘れるな
2.82

git add .
git commit -m "changes"
git push origin main

stripe login
stripe listen --forward-to http://localhost:3000/api/stripe-webhook
stripe trigger payment_intent.succeeded
