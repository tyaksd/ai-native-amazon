printful　api(最悪手動)

オーダーデーTA（サイズ、個数、色)作る、checkoutの時点で、でもstripeが承認するまでfalse

データを新しく作るときはsupabase sqlコードを忘れるな

ブランド商品追加も自動でできるようにapi画像も
openai api繋げる

いいねで保存できるようにする

決済できてない

各ストアを運用するAIエージェント

　godshipのx instagram運用AIエージェント

デザインcopilotモードこれは後でいい

世の中のブランドと商品を大量に学習するai
トレンド定量化ai
クリエイティブ定量化ai

ブランドページのall newページ切り替え反応しない

最初自動で流れ出す、途中でユーザーがスクロールに介入するとユーザー任せ、ユーザーがスクロールをやめるとそこからまたスクロール再開について、どうしても
できなかったので、自動スクロールだけにした

非PCの時、ヘッダーにsearchロゴが表示されるが、押しても何も反応しないので、押すとヘッダーの全てがsearchの文字入力欄になってスマホ/ipadの入力が起動するようにして、



<＜ルール＞＞
カラーは全部大文字で

<<<<<<githubの変更がvercelでディプロイしているコードに自動で反映されない時>>>>>>>>
1. Git に正しい author を設定
まず GitHub に登録しているメールを確認します（例：yourname@gmail.com や xxxx@users.noreply.github.com）。
git config --global user.name  "あなたの名前"
git config --global user.email "GitHubに登録しているメール"
2. 設定を確認
git config --global --list
で user.email が GitHub のメールになっているか確認。
3. コミットを修正
すでに push したコミットが「.local」になっているので、最新のコミットだけ直したいなら：
# 最新コミットの author を修正
git commit --amend --reset-author --no-edit
# 修正を force push
git push origin main -f
これで最新コミットの author が正しい GitHub メールに置き換わります。
<<<<<<<<<<>>>>>>>>>>

git add .
git commit -m "changes"
git push origin main